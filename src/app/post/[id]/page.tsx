import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Post",
  description: "View additional post information.",
};

const SinglePostPage = () => {
  return (
    <main>
      <h1>Post Page</h1>
    </main>
  );
};

export default SinglePostPage;
